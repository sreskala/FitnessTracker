@model FitnessTracker.Models.WorkoutModels.WorkoutPlan.WorkoutPlanDetail

@{
    ViewBag.Title = "Delete";
}
@section Styles {
    <link href="@Url.Content("~/Styles/DetailsView.css")" rel="stylesheet" type="text/css" />
}

<div class="padding-top-view">
    <h2 class="title-header page-header">@ViewBag.Title</h2>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Workout Plan #@Model.WorkoutPlanId</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.WorkoutPlanId)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.WorkoutPlanId)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Title)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Title)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.DateCreatedUtc)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.DateCreatedUtc)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.DateModifiedUtc)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.DateModifiedUtc)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
</div>