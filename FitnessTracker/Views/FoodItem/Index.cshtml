@model IEnumerable<FitnessTracker.Models.MealModels.FoodItemModels.FoodItemListItem>

@{
    ViewBag.Title = "Food Items";
}

@section Styles {
    <link href="@Url.Content("~/Styles/DetailsView.css")" rel="stylesheet" type="text/css" />
}

<div class="padding-top-view">
    <h2 class="page-header title-header">@ViewBag.Title</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>

    @if (Model.Count() < 1)
    {
        <h3>Looks like your Food Items are empty! Hit create new to start adding some.</h3>
    }
    else
    {
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.FoodItemId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.MealId)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.FoodItemId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.MealId)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.FoodItemId }) |
                        @Html.ActionLink("Details", "Details", new { id = item.FoodItemId }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.FoodItemId })
                    </td>
                </tr>
            }

        </table>
        <input type="button" value="Add Food Items to Meals" onclick="location.href='@Url.Action("AddFoodItemsToMeal","FoodItem")'" />
    }

</div>